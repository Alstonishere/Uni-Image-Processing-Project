Prefer run on terminal which has Python Environment (Eg: Visual Studio Code, Anaconda Prompt, CLI)

Step
1. Type "pip install requirement.txt" in terminal
2. Type "streamlit run login.py" (If cannot work try "python -m streamlit run login.py")
